/**
 * @author Alefas
 * @since 13/04/15.
 */
object helper {
  val methods = "methods"
  val more = "more"
  val Number = new { def of(a: Any) = new { def is(i: Any) = new {def than(i: Int) = i} } }
}
